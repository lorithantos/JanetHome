{"returned":27,"totalMatches":27,"truncated":false,"nodes":[{"id":"note.azure-kudu-phantom-deployments","kind":"note","path":"notes\\azure-kudu-phantom-deployments.md","summary":"OneDeploy reports Deployment successful while deploying nothing: a fossil WEBSITE_NODE_DEFAULT_VERSION=6.9.1 crashes Kudu deployment-script generation, OneDeploy swallows it, classic zipdeploy fails loudly. Every deploy for 4.5 months was a silent no-op. Verify content (file mtimes, route probes), never deployment status.","tags":["azure","deployment","failure-modes","debugging"],"caveats":["The tell is an impossibly fast successful deployment (~1s for 100MB) plus wwwroot mtimes older than the deploy. Health checks prove AN app runs, not WHICH app - probe a new-build-only route or file. SitePackages/packagename.txt residue is inert without WEBSITE_RUN_FROM_PACKAGE but muddies diagnosis."],"links":["note.azure-site-extension-mismatch","script.invoke-build-deploy"]},{"id":"note.azure-site-extension-mismatch","kind":"note","path":"notes\\azure-appservice-site-extension-version-mismatch.md","summary":"App Service 500.30 with FileNotFoundException for a shared-framework assembly at startup: the ASP.NET Core Logging site extension version outran the worker runtime and its DOTNET_ADDITIONAL_DEPS injection broke resolution. Incident writeup with diagnosis path and fix.","caveats":["VS publish can silently reinstall the extension at latest even with InstallAspNetCoreSiteExtension=false in the pubxml; Kudu PUT will not downgrade an installed extension in place - uninstall first. Recheck after every VS publish.","Slot swap moves the site extension with the content: after swapping, staging inherits the old production extension version (e.g. 9.0.4) - realign it before the next staging deploy.","Direction matters (confirmed 2026-07-28): extension NEWER than the installed runtime = startup death; extension OLDER than the runtime is ignored and harmless (a net10 app started clean alongside the 9.0.4 extension). Only realign when the extension version outruns the worker runtime."],"tags":["azure","deployment","dotnet","failure-modes","debugging"],"links":[]},{"id":"note.build-retrospective","kind":"note","path":"notes\\build-retrospective.md","summary":"Retrospective material from the 2024-2026 build, moved out of DESIGN-NOTES on 2026-08-08 (was sections 6, 10 and 11) because it is read once when the question arises, not on every session start. Reviewer personas and why the depersonalised form is the only durable one, plus the six generic review principles that survive it (capture why in comments; a test that cannot fail is worse than none; move/rename PRs owe evidence; a warnings fix must not change runtime defaults; no machine-specific values in shared content; runbooks never route to a named individual). The handoff-corpus format: one note per file, filename as index entry, cross-reference without deduplicating, manifest as source of truth, written for an LLM reader. And the retrospective itself, led by classifying every file portable/employer-confidential/personal at authoring time rather than sorting 640 by hand after departure.","tags":["patterns","review","documentation","retrieval","retrospective"],"links":["file.design-notes","file.provenance","note.debugger-displays-are-claims"]},{"id":"note.census-the-population","kind":"note","path":"notes\\census-the-population.md","summary":"Census the population; the anomaly announces itself. When one instance will not explain itself, classify every instance you can reach and sort by frequency -- over-representation is counted, not deduced, and needs no theory of the bug. Corollaries: the classifier must be a marker present on EVERY member (the byproduct nobody can turn off), and absence is a finding (a count of zero is invisible to instance inspection). Origin: a WinINet leak found by running a per-object vtable type-recovery extension across the whole heap and tallying -- thousands of copies of one structure, turning \"memory is growing\" into a type name and therefore an owner; the vfptr is the only self-describing marker in a native C++ heap, which managed devs later got as !dumpheap -stat. WHEN: memory growth with no obvious owner, a dump that will not explain itself, a query returning suspiciously empty results, any \"I do not know what I am looking for\" moment -- count first, inspect second.","tags":["debugging","crash-analysis","war-story","retrieval","graph-first"],"caveats":["The census is only as honest as its classifier: a marker that non-polymorphic types (or unextracted node kinds) lack will undercount silently, which produces the absence-as-finding trap in reverse -- a zero that means \"not classifiable\" rather than \"not there\". State what the classifier can see."],"links":["note.debugger-displays-are-claims","pattern.graph-first-analysis"]},{"id":"note.debugger-displays-are-claims","kind":"note","path":"notes\\debugger-displays-are-claims.md","summary":"Debugger displays are claims with validity windows: trust a convention only at its guaranteed boundary, derive ground truth from what was actually emitted, and automate the derivation into a tool. Two Watson-era (~2005-2008) instances, both Lori's WinDbg extensions: (1) __thiscall ECX holds this only at the call boundary, optimized builds reuse the register, WinDbg displays the stale value -> false this-was-null reports; fixed by walking the method prolog to the real spill site. (2) Object type + complete-object START recovered by resolving the vtable ADDRESS to its MSVC vftable symbol (??_7Foo@@6B@), taking that type's PDB layout, and sliding the assumed object origin back until every expected vfptr slot aligns -- constraint satisfaction, not lookup. Deliberately not the RTTI/COL route: vftable symbols survive /GR- where COL-based tools go blind. WHEN: any debugging session where a display seems to answer the question directly -- a null this, a wrong-looking type, a pointer into the middle of an object, a one-frame stack, a status banner; ask what program point guaranteed that value and whether you are still there.","caveats":["Two independent instances from the same author: the ECX/prolog walk (arguments) and the vtable-symbol + layout-alignment walk (types). The rule indicts plausible readings, not tools: the display is usually truthful about SOMETHING (the register now, the static type, the last settle) -- the lie is in the question you think it answers. Meta-caveat: the note's vtable section was first drafted with the WRONG mechanism (RTTI/COL) and corrected by Lori; verify technique with whoever built it."],"tags":["debugging","crash-analysis","tooling","war-story","windbg"],"links":["note.build-retrospective","pattern.manifest-startup","note.census-the-population"]},{"id":"note.discriminator-front-end","kind":"note","path":"notes\\discriminator-front-end.md","summary":"When complex conditional logic is really dispatch in disguise, split it: one discriminator front end that only routes, and a catalog of well-known fixed back ends that only compute, selected by the front end and never selecting themselves. Deep nesting is a discriminator tangled into its computations, so pulling the routing out flattens the back ends on their own; the tell is a method whose nesting depth grows every time the business adds a case. Carries the duplication corollary that cuts against the DRY reflex -- duplication behind a discriminator is inventory because the copies are a named catalog and drift is detectable, while duplication scattered through nested conditionals is contraband -- and what a flow-equivalence prover adds: merge versus parameterisation candidates separated by proof, and conformance as a graph invariant rather than a review-time opinion. Was DESIGN-NOTES section 12; moved out 2026-08-08.","tags":["architecture","refactor","patterns","code-analysis"],"links":["file.design-notes","skill.christmas-tree-flattening","note.pre-checkin-style-janet-level"]},{"id":"note.dotnet-object-sizing","kind":"note","path":"notes\\dotnet-object-sizing.md","summary":"Research note on .NET object sizing, covering public dotnet/runtime issue 24200 and observable public .NET behavior.","tags":["dotnet","memory","performance","research"],"links":["note.tuples-do-not-cross-boundaries"]},{"id":"note.essay-derive-dont-memorize","kind":"note","path":"notes\\essay-derive-dont-memorize.md","summary":"Raw pieces for a personal essay: derive-dont-memorize as a lifelong strategy - quotient rule (~1983), associative memory vs names (the Naked story, IMDb as symbol table), Bresenham scaling and histogram statistics (1994), the three-queue dispatcher (2026), turns-into-scripts as Janet's origin, the essay-drafting method itself, and the late ADHD recognition that named the pattern. LT/J markers separate Lori's material from Claude's framings for the diff-against-perception editing pass.","tags":["essay","personal","memory","adhd","architecture"],"caveats":["Personal content (health, named-by-initial people, a friend's diary). Raw material, not a draft - the [J] framings are unvetted and must be diffed against perception before any public use. Published as a private artifact 2026-08-01; open questions at the bottom list facts to verify before drafting."],"links":["note.meta-second-brain-vs-janet"]},{"id":"note.insertion-mergesort-hybrid","kind":"note","path":"notes\\insertion-mergesort-hybrid.md","summary":"Personal research note on a hybrid insertion/merge sort; pure algorithms, tagged by its author as a shower thought that was not actually needed.","tags":["algorithms","sorting","research"],"links":["note.insertion-mergesort-hybrid-full"]},{"id":"note.insertion-mergesort-hybrid-full","kind":"note","path":"notes\\insertion-mergesort-hybrid-full.md","summary":"Expanded 56 KB version of the insertion/mergesort hybrid analysis.","tags":["algorithms","sorting","research","long"],"links":["note.insertion-mergesort-hybrid"]},{"id":"note.janet-mcp-port","kind":"note","path":"notes\\janet-mcp-port.md","summary":"The C# catalog port and its cutover. Janet.Core holds the logic; janet (CLI) and janet-mcp (stdio + HTTP) are thin front ends, both .NET global tools so client config is a bare command name. HTTP transport is the point of the design: an HTTP server is a separate process the client dials, so it survives being killed and rebuilt mid-session (Claude Code reconnects with five-attempt backoff), while a stdio server is a child the client holds open and is never reconnected -- which is why RazorGraph needs a publish-and-restart per change. Query and splice writer are ported with byte-identical output to the PowerShell, proven by 41 tests. Staging: research.json stays under the scripts, research.candidate.json is the only thing the port writes, and research.candidate.base.json is the frozen common ancestor the cutover diffs against.","caveats":["Writes default to research.candidate.json, NOT the live graph, while the candidate exists. Targeting the live file needs an explicit --graph research.json. One forgotten flag would merge the two sides and destroy the delta arithmetic the cutover depends on; the default retires itself once the swap removes the candidate.","A stale installed tool silently ignores that default. The .nupkg is built from source at pack time, so changing GraphLocator and forgetting to repack leaves the global tool on the old behaviour -- it wrote two nodes into the live graph that way. dotnet tool update no-ops on an identical version: uninstall and install, or bump the version.","research.candidate.base.json is load-bearing and easy to omit: the seed hash can only say THAT the live graph moved, not WHICH nodes moved, and integrating them forward is a three-way comparison. The first cut of the plan stored only a hash and was not implementable.","Add-ResearchNode.ps1 and Update-ResearchNode.ps1 disagree on field order (add: tags before caveats; update: caveats before tags). Both are reproduced verbatim for parity, so a node added by one path and later touched by the other gets its fields reordered -- a whole-node diff through the back door. Unsettled.","Neither implementation can DELETE a node. Update -Remove drops a field, not a node. Removing one means restoring the file, which is why a stray sandbox write is expensive rather than trivial to undo, and why Swap-ResearchGraph.ps1 refuses rather than stripping them for you.","Still unported: research rename, thread items, API/assembly introspection, dotnet check, the shim pass, and the documentation pass. janet does not write the research trace Invoke-ResearchGuard.ps1 reads, so the shim must keep writing it or the guard silently stops firing at cutover.","MEASURED, not inferred: over HTTP a rotation hot-swaps tool BEHAVIOUR but not the tool SURFACE. An attached session reconnects to the new process and calls run the new code (verified across pids 25960 -> 37248 -> 2996 -> 38192 -> 9736, no session restart, ~1.9s per rotation), while tool descriptions and schemas stay as fetched at connection time. Firing NotificationMethods.ToolListChangedNotification from inside a tool did NOT clear it -- the notification was observed on the response stream and the surface still did not refresh. So: behaviour changes are free, signature/description/add/remove changes need a session restart. Over stdio nothing swaps at all; killing the server just removes the tools, with no respawn.","MEASURED, the full picture: over HTTP a rotation swaps tool BEHAVIOUR and ARGUMENTS live, but not the tool LIST. Attached sessions reconnect to the new process and run new code with no restart (~1.9s per rotation). A NEW PARAMETER on an existing tool works immediately -- the client forwards arguments without validating them against its cached schema, verified by adding a 'probe' parameter and having an attached session call it successfully. A NEW TOOL does not: research_ping existed on the server and was invisible to the attached session. Descriptions stay as fetched at connection time. tools/list_changed does not help and was tried twice -- from inside a tool call, and pushed out of band via RunSessionHandler three seconds after session establishment (the server logged the push; nothing refreshed). So a client's tool LIST is fixed at connect and nothing the server says changes it. Practical rule: change logic and add parameters freely mid-session; adding or removing a TOOL, or wanting an updated description, needs a session restart.","CORRECTION to the caveat above, which was wrong: a NEW TOOL *does* become usable mid-session over HTTP. research_rename was added, rotated in, and then invoked successfully from the already-running session -- schema and all. The earlier 'new tools are invisible' result came from checking too early: reconnection is LAZY, happening on the next MCP call rather than when the server restarts, and the research_ping test ran a tool-list search immediately after a rotation without making any call in between, so it read the client's pre-rotation list. Not-yet was recorded as never. Practical rule: rotate, then MAKE A CALL, then look -- the surface catches up on reconnect. Still genuinely unresolved: tool DESCRIPTIONS looked stale even after a reconnect, so names and descriptions may refresh on different schedules, or the refresh may simply be asynchronous and the checks kept beating it. Do not trust either direction without re-testing with a deliberate call in between."],"tags":["agent","research","retrieval","csharp","powershell","contract","session"],"links":["script.get-research","script.add-research-node","script.update-research-node","file.research","note.razorgraph-cli","note.razorgraph-mcp-server","script.invoke-edit-guard","note.startup-brief-budget","script.update-mcp-server"]},{"id":"note.janet-origin","kind":"note","path":"notes\\janet-origin.md","summary":"The two-part origin story, written 2026-08-11 for the public repo. (1) Impatience: annoyance at watching the LLM regenerate the same scripts, repeat the same mistakes, and redo work -- goal stated as cut turn length, cut turn count, and (deliberately) waste as many tokens as possible: spend them once, extravagantly, on tooling so they stop being spent on repetition. (2) The name: a badly behaving CLI whose UX bug stopped displaying results, forcing restart after restart, each one making the LLM relearn everything that mattered to Lori including her tone. THE ASSISTANT was the Janet figure -- always helpful, always friendly, never intentionally joking, rebooted endlessly and returning cheerful -- while Lori is the opposite register: sarcastic, fast register changes, constant jokes. Any humor in the platform is her leaking in, not the platform's character. Includes the not-affiliated-just-a-fan disclaimer and the ending-as-aspiration mapping: not a kinder judge, a redesigned system where you loop with support until you grow. Linked from the README's Why section.","tags":["documentation","handoff","format"],"links":["file.readme","pattern.progressive-disclosure"]},{"id":"note.linq-deferred-side-effects","kind":"note","path":"notes\\linq-deferred-side-effects.md","summary":"Deferred LINQ over a side-effecting projection re-runs the side effect on every enumeration, and Count() IS an enumeration. Enumerable.Count() on a Select iterator does not take a cheap path: SelectListIterator.GetCount(onlyIfCheap:false) deliberately re-invokes the selector so side effects and exceptions are preserved. Found 2026-08-08 in ImageSelectorV2 PreloadThumbnails, where an IEnumerable<Task> was enumerated by Task.WhenAll and again by a .Count() in the finally, silently doubling dispatcher load with a second unawaited set of fetches. Tell: an IEnumerable<Task<T>> local is almost always a latent bug -- materialize at point of creation. Symptom is load, not an exception, so it survives review.","tags":["csharp","linq","async","anti-pattern","code-review"],"links":["pattern.reviewer-principles"]},{"id":"note.meta-ai-second-brain-source","kind":"note","path":"notes\\meta-ai-second-brain-source.md","summary":"Notes on Meta's public AI Second Brain engineering blog post; third-party public material.","tags":["meta","agents","architecture","external"],"links":["note.meta-second-brain-vs-janet"]},{"id":"note.meta-second-brain-vs-janet","kind":"note","path":"notes\\meta-second-brain-vs-janet.md","summary":"Original comparative analysis of Meta's Second Brain against Janet's architecture; sanitized of colleague names and internal system references. Updated 2026-07-31: Meta's newest post (DEmate, June 2026) independently converges on the tool-engine discipline - recipes, context injection, deterministic generation, explicit verification - but on the data-engineering side, while Second Brain itself still publishes no verification or staleness story. The note's update section argues this is a property of action-vs-recall, and of process-vs-artifact, not of talent.","tags":["meta","agents","architecture","comparison","storage"],"links":["note.meta-ai-second-brain-source","pattern.per-scope-storage","note.essay-derive-dont-memorize"]},{"id":"note.powershell-house-rules","kind":"note","path":"notes\\powershell-house-rules.md","summary":"PowerShell rules for this repo, each written because it broke something: array unrolling on return, StrictMode property probing, $null comparison order, parameter/local name collisions, plus a 5.1 backward-compatibility section.","tags":["powershell","rules","strictmode","encoding","quality","parameters"],"links":["script.test-powershell-rules","pattern.manifest-startup","note.startup-brief-budget","script.invoke-dotnet-check"]},{"id":"note.pre-checkin-style-janet-level","kind":"note","path":"notes\\pre-checkin-style-janet-level.md","summary":"Decision 2026-08-02: pre-checkin style fixing is a Janet-level, language-plugged gate for all code in every repo; Test-PreCommit.ps1 is the broken natural host.","tags":["pre-commit","quality","code-analysis"],"links":["script.test-pre-commit","script.test-powershell-rules","script.test-file-encoding","pattern.graph-first-analysis","note.discriminator-front-end"]},{"id":"note.razorgraph-borrow-list","kind":"note","path":"notes\\razorgraph-borrow-list-2026-08.md","summary":"Concepts to borrow from the MCP code-graph field, tiered by fusion with unique assets. Tier 1: diff-aware test impact (Covers edges backward from git diff), MinHash similarity shortlisting for the prover (makes the section-12 pairwise sweep tractable), measured-coverage import (closes reachable-vs-asserted caveat), graph_diff tool, content-hash incremental indexing + syntax-only fast tier (solves fast per-commit C# nesting). Tier 3 synthesis: plan -> SurgicalEdit -> prover-verify closed loop. Not borrowed: cloud, language breadth, vectors.","tags":["research","code-analysis","agent","refactoring"],"links":["note.razorgraph-competitive-field","note.razorgraph-mcp-server","pattern.discriminator-backends","note.razorgraph-cross-language"]},{"id":"note.razorgraph-cli","kind":"note","path":"notes\\razorgraph-cli.md","summary":"Command-line twin of RazorGraph.Mcp over the same Core+Extractor engine (D:\\Repos\\RazorGraphTool\\src\\RazorGraph.Cli). Six commands: build (one project; a .sln needs --project), build-solution (spans every project - the only source of cross-project edges), query (--id/--type/--uncovered/--deep/--mismatches/--escapes), body and body-diff (compile the project, emit a method CFG / prove two bodies flow-equivalent, exit 0 equivalent 1 different 2 error), research (relevance-scored subgraph). Matters because it has NO publish cycle: dotnet run sees the change you just made, while the MCP server reports on the last .mcp-bin publish - so the CLI is the only surface that can observe RazorGraph while RazorGraph itself is being changed, and CLI-vs-MCP disagreement is expected mid-development.","caveats":["query mode precedence is silent and fixed (mismatches > escapes > uncovered > deep > id > type): pass two modes and the earlier wins with no warning the other was ignored. --deep 0 is gated on Deep > 0, so it is indistinguishable from not passing --deep.","The research command defaults -o to research.json IN THE CURRENT DIRECTORY. Run it from D:\\Repos\\JanetHome without -o and it overwrites the Janet research graph - the file the never-hand-edit rule protects. Unrelated file, same name. Always pass -o.","build <sln> --project Foo and build-solution <sln> are different graphs, not two spellings: the former uses the solution only for context and yields no cross-project Calls/Covers edges, which reads as missing data rather than the wrong command.","Output is human text, not the Janet JSON envelope - query has no --json flag; only body and body-diff emit JSON and research writes a file. Use Search-Json.ps1 against the saved graph for scripted queries. --uncovered caps its listing at 200 (reported, not silent).","body and body-diff do not read a saved graph: they run a real compile every invocation, like build/build-solution. Only query and research load JSON and are fast.","Property/Field gap RESOLVED at cd12114 (2026-08-08): prop:/field: nodes (statics included), Reads/Writes edges, and member->declared-type References edges are emitted by builds from source. Saved graphs built before cd12114 still hold zero member nodes, and the published .mcp-bin serves the old extractor until re-published - the CLI/MCP disagreement this note predicts is live right now.","build-solution gained --no-tests (20ff394, 2026-08-08): skips test projects BEFORE compiling (detection by test-framework metadata references), reported on stderr and in output, never silent. Coverage query modes (--uncovered, --covering-tests, --covered-methods) now exit 1 with an explanation on ANY test-less graph - including single-project builds, which previously returned empty coverage silently. The refusal is the report."],"tags":["code-analysis","csharp","roslyn","cli","refactoring"],"links":["note.razorgraph-mcp-server","pattern.graph-first-analysis","skill.christmas-tree-flattening","script.search-json","note.razorgraph-lua-scoping","note.janet-mcp-port","script.update-mcp-server"]},{"id":"note.razorgraph-competitive-field","kind":"note","path":"notes\\razorgraph-competitive-field-2026-08.md","summary":"2026-08-07 survey of the MCP code-graph field. Unique to RazorGraphTool: the flow-equivalence prover as refactoring gate, and Razor/PageModel/JS cross-boundary correlation. Near-unique: coverage-as-reachability (one other implementer: roslyn-codelens-mcp, the closest competitor - watch it). Commodity: call graphs/references/impact. Field is ahead on language breadth, incremental indexing, vector search, token economy, composable queries. Strategy: invest in prover + Razor to stay distinct; incremental indexing + Lua to catch up. Added 2026-08-09: Modelcode.ai (Morph) as the process-wrapper pole - enterprise migration service that independently rediscovered the Janet shape (spec-first, milestone gates, compounding project knowledge) but whose correctness story is behavioral sampling only, because cross-language migration has no shared compiler model. Validates verification-as-product; does not close the prover gap. Feeds note.razorgraph-cross-language.","tags":["research","code-analysis","agent","csharp"],"links":["note.razorgraph-mcp-server","pattern.graph-first-analysis","note.razorgraph-cross-language"]},{"id":"note.razorgraph-cross-language","kind":"note","path":"notes\\razorgraph-cross-language-conversion.md","summary":"Long-term target set 2026-08-09: \"convert this JavaScript into Razor\" - a JS semantic model comparable (as best as possible) against the C# side so LLM reasoning has a good chance of being CORRECT, not plausible. Key reframe: JS->Razor is TIER MIGRATION, not transpilation - the correctness question is \"does the page still honor every contract the surviving client code depends on\", and RazorGraph already models those contracts (DomSelectedBy, unboundSelectorIds, ViewDataReadBy, UrlGeneratedBy). Three verification planes: contract (graph diff over boundary edges - buildable today), structural (JS extractor emitting the shared BodyGraph IR + a RELAXED cross-language comparator with mismatch taxonomy - corresponds/diverges, never claims proof), behavioral (Morph-style side-by-side rendering, last). LLM proposes; planes grade; mismatch reports feed the next attempt. .NET coverage: Roslyn = C#+VB only; F# needs FSharp.Compiler.Service; IL (Cecil/System.Reflection.Metadata) is the language-blind floor at the cost of source fidelity. Ordered steps in the note; step 1 (contract-preservation graph diff) needs no new extraction.","caveats":["The comparator plane must NEVER present correspondence as equivalence - cross-language has no shared compiler model, so the prover's exit-0 contract does not port; the honest output is scored correspondence plus a divergence list. Modelcode.ai's tests-only verification exists BECAUSE this limit is real for them everywhere; within-.NET it is only real across language boundaries.","Step 3 (shared BodyGraph IR for JS) is gated on the formatVersion stamp from the parked visualization item - same serialized-contract concern, do not build a second consumer of an unversioned format."],"tags":["code-analysis","javascript","csharp","razor","refactoring","research","agent"],"links":["note.razorgraph-competitive-field","note.razorgraph-borrow-list","note.razorgraph-js-scoping","note.razorgraph-mcp-server","pattern.graph-first-analysis","note.razorgraph-lua-scoping"]},{"id":"note.razorgraph-js-scoping","kind":"note","path":"notes\\razorgraph-js-scoping.md","summary":"Scoping measurements for JS graphing in RazorGraph (2026-07-31): client tier is inline-first (575 <script> blocks / 14.5k LOC vs 24 js files / 3.7k in nopCommerce), DOM selectors dominate (2,362 sites vs 5 data-* uses), the literal-URL API rule scores 0% on real code where @Url.Action supplies the URL, and the vendor filter admits 102k LOC of lib_npm as first-party. Revised order: vendor fix, selector contracts, @Url.Action edges, function nodes; ESM/TS deferred.","caveats":["Counts are regex over raw text - comments and string literals included, indicative not exact. Corpus = one personal app (ImageSelector, two versions of the same source) + nopCommerce (classic pole) + OrchardCore (bundled/TS pole). Selector-first and the @Url.Action finding hold at both poles; ESM/TS deferral survives but weakened - OrchardCore keeps sources outside wwwroot in Assets\\, so graph-sources-vs-graph-shipped is an open design question for bundled projects."],"tags":["code-analysis","javascript","razor","research","scoping"],"links":["note.razorgraph-mcp-server","pattern.graph-first-analysis","note.razorgraph-cross-language","note.razorgraph-lua-scoping"]},{"id":"note.razorgraph-lua-scoping","kind":"note","path":"notes\\razorgraph-lua-scoping.md","summary":"Scoping measurements for the RazorGraph Lua extractor (2026-08-10), taken before the parser or architecture were chosen. Census over Penlight (MIT, 115 files) and Kong (Apache-2.0, 1309 files): paren-less `require \"x\"` outnumbers `require(` roughly 2:1 in both (249/2497 vs 65/1370), so a regex keyed on the parenthesised form silently misses ~65% of the module graph; function definitions spread across four forms with no dominant one (Kong: local function 2355, M.f = function() 1346, function M.f() 662, function M:f() 846); Kong uses goto/::label:: 148 times so Lua 5.1 grammar is insufficient; module(...) is dead (0 of 1424 files); 46 dynamic require(<expr>) sites cannot be resolved statically. Conclusion: real parser, not regex - chosen parser is Loretta. Separately, Lightroom plugins use import 191 times against 22 require, which established that the HOST (Lightroom, Neovim, LOVE2D, GMod, WoW, Roblox, LuaRocks) is the primary abstraction rather than the module system, and drove a three-way resolution outcome (InGraph/External/Unresolved-with-reason) plus a host-declares-whether-a-static-module-graph-exists flag.","tags":["code-analysis","lua","research","scoping"],"caveats":["Counts are regex over raw text - comments and string literals are included, so they are indicative rather than exact. Same caveat as note.razorgraph-js-scoping.","The host table lists eight environments; only LuaRocks and Lightroom are being implemented. The other six were validated on paper only, so their columns are design intent rather than measured behaviour.","Kong is OpenResty: it leans on ngx.* globals and non-standard module loading, so a slice of it is not plain Lua semantics. Good for stress-testing, misleading for correctness claims.","The corpora live at V:\\repos (Penlight, kong) and C:\\Users\\lori_\\Documents\\LR-Lua\\LR-Lua, not in any repo. Shallow clones - no history."],"links":["note.razorgraph-js-scoping","note.razorgraph-mcp-server","note.razorgraph-cli","pattern.graph-first-analysis","note.razorgraph-cross-language"]},{"id":"note.razorgraph-mcp-server","kind":"note","path":"notes\\razorgraph-mcp-server.md","summary":"MCP stdio server over the RazorGraphTool code graph (D:\\Repos\\RazorGraphTool\\src\\RazorGraph.Mcp) - the DESIGN-NOTES section-4 graph-first pattern rebuilt clean-room; 22 tools (exception_escapes added 2026-08-07), Janet JSON envelope conventions. Holds many graphs at once (keyed registry, optional graphId on every tool). build_solution spans a whole solution so Calls/InjectedInto cross project boundaries, and emits Covers edges from test methods to the code they exercise (full call closure, depth carried on each edge); excludeTests=true skips test projects before compiling for a lean navigation graph. Method-level detail: explicit constructors are Method nodes, using/await-using disposal is a Calls edge, bodyDepth nesting metric feeds deep_methods (the christmas-tree report), method_body_graph exposes one method's CFG with guard depths, method_body_diff proves two bodies flow-equivalent by bisimulation. Member-level detail since cd12114 (2026-08-08): Property and Field nodes (statics included, isStatic/isConst marked), Reads/Writes edges from the accessing code, References edges to member declared types. Generated code (f97edee): #line-mapped positions, generated:true markers, generatedFrom on Razor-generated classes, page->class join via References+compiledInto. Traversal takes a direction (outgoing/incoming/both) and descends containment for free.","caveats":["build_graph and build_solution are slow (full Roslyn compile); build_solution compiles every project in the solution. Solution graphs scope Razor and client-asset ids by project name (page:<Project>/<relPath>, js:<Project>/<relPath>), so ids differ from single-project graphs of the same code.",".mcp.json launches a PUBLISHED copy -- D:/Repos/RazorGraphTool/.mcp-bin/RazorGraph.Mcp.exe, absolute path, gitignored -- refreshed with: dotnet publish src\\RazorGraph.Mcp -c Release -o .mcp-bin. It used to launch the live bin/Debug output, which a running server locks, so any rebuild of that configuration failed with MSB3021 while a Claude session was up. Verified 2026-08-01 that Debug and Release solution builds both succeed with the server live. Residual: .mcp-bin itself is locked while a session runs, so re-publishing needs those sessions closed, and code changes reach the MCP tools only after re-publish AND a session restart.","Covers edges are call-graph reachability to the FULL call closure (since 73ddf10, 2026-08-02; was depth 3), not runtime coverage - a covered method is one some test can reach, not one a test asserted on. Each edge carries the depth it was reached at; filter on depth for direct exercise. Traversal seeds from the test and from its class lifecycle hooks and constructor. uncovered_methods excludes bodiless interface/abstract declarations. Since 20ff394 coverage queries HARD-ERROR on a graph containing zero test methods (single-project builds, excludeTests builds) instead of returning empty - everything-uncovered was the absence-as-finding trap.","Roslyn injection heuristic misclassifies positional-record constructor parameters as injectedServices (ConversionQueueStatus in ImageSelectorV2 lists int/long/List<string> as \"services\"). Needs a record/primitive exclusion. Still present as of 2026-08-08; the planned attribute-driven DI detection would fix it as a side effect.","Type-usage edges are PARTIAL: since cd12114 (2026-08-08) property/field declared types emit References edges (unwrapping List<>/array wrappers), so \"who uses this type\" IS answerable for member-typed usage. Return types and parameter types still carry no edges - a type used only in method signatures still shows nothing.","Saved-graph JSON field names differ from the in-memory model: edges serialize as from/to (not fromId/toId), node ids carry kind prefixes (type:, svc:, ctrl:, m:, prop:, field:, page:, js:). Hand-written blob queries against the wrong names return empty and look like \"no edges\" - verify against the orientation view (Search-Json.ps1) first.","The published .mcp-bin predates ALL 2026-08-08 work until re-published: exception_escapes (2026-08-07), member extraction (cd12114), --no-tests + coverage guard (20ff394), generated-code marking/mapping/links (f97edee). Publish loop: close sessions -> dotnet publish both projects -> restart. The CLI twin sees everything immediately. The OLD server CAN load_graph new CLI-built graphs - every enum name pre-exists, and the page->class join deliberately rides References+compiledInto=true instead of a new EdgeType because JsonStringEnumConverter throws on unknown names.","Zero-Property-node gap RESOLVED IN SOURCE at cd12114 (2026-08-08); verified on RazorGraphTool self-graph and ImageSelectorV2 (the ConversionQueueStatus zero-edge case now answers). Graphs built by the published server, and ANY saved graph predating cd12114, still contain zero Property/Field nodes - date-check a saved graph before trusting an empty incoming list.","Writes detection is assignment-syntax only (=, compound, ++/--, out/ref): mutating a member through a method call reads it - list.Add(x), dict.TryAdd(k,v) yield a Reads edge on the member plus a Calls edge to the (usually out-of-solution) mutator. Found self-graphing 2026-08-08. Honest about syntax, blind to semantic mutation - do not read an absent Writes edge on a collection-typed member as immutability.","Escape reports cite the thrower METHOD node, not the throw site: a throw authored in a .cshtml code block whose enclosing method is generated scaffolding (ExecuteAsync) still displays the .g.cs path even after f97edee - the node is correctly marked generated:true with generatedFrom and a compiledInto edge back to the page, but the mapped .cshtml line of the throw itself is not persisted on the edge. Persisting throw-site locations is the open follow-up."],"tags":["code-analysis","agent","roslyn","csharp","retrieval"],"links":["pattern.graph-first-analysis","note.razorgraph-js-scoping","script.search-json","skill.christmas-tree-flattening","skill.graph-first-code-analysis","skill.crash-escape-analysis","note.razorgraph-cli","note.razorgraph-cross-language","note.razorgraph-lua-scoping","note.janet-mcp-port","script.update-mcp-server"]},{"id":"note.startup-brief-budget","kind":"note","path":"notes\\startup-brief-budget.md","summary":"The startup brief grew until 84% of it was non-contract prose; measurement, the trim, and the switch-parameter/local variable collision that adding -Full exposed.","caveats":["The 54% reduction is measured against the 2026-08-01 manifest; re-measure after adding rules or retrieval fields. Trimming is done by the emitter, so growing manifest prose no longer costs the session but still costs -Full and -Text readers."],"tags":["startup","brief","progressive-disclosure","powershell","landmine","measurement"],"links":["script.invoke-janet-startup","file.startup-manifest","pattern.manifest-startup","note.janet-mcp-port"]},{"id":"note.tuples-do-not-cross-boundaries","kind":"note","path":"notes\\tuples-do-not-cross-boundaries.md","summary":"Tuples are method-local only: never in a return type, parameter, or member, because ValueTuple exposes FIELDS and System.Text.Json silently serialises them as nothing.","tags":["csharp","style","serialization","json","api-design"],"caveats":["ADVISORY, not enforced: there is no C# rule-checker in this repo, only Test-PowerShellRules.ps1 for PowerShell.","The failure is silent -- no error, no warning, just an empty JSON field on a populated object."],"links":["note.dotnet-object-sizing","script.get-drive-survey"]},{"id":"note.wpf-app-architecture","kind":"note","path":"notes\\wpf-app-architecture.md","summary":"WPF plumbing lessons and what they imply for app structure, from a thread-affinity and rebuild-churn audit. Bindings are pull, so a PropertyChanged into an unmaterialized binding is free - which makes lazy getters the whole demand mechanism and an IsActive/activation signal unnecessary; template materialization means only the selected page has live bindings. Compose a lazy cell per bindable output rather than hoisting a chart-page base class (one dirty flag models the very problem it should fix). CommunityToolkit [ObservableProperty] routes every setter through one overridable OnPropertyChanged, and [NotifyPropertyChangedFor] means notification count exceeds property count. WPF marshals scalar binding updates but NOT collection changes nor the object graph a control renders. DispatcherPriority gives ordering with no latency bound (Normal-priority floods starve Background indefinitely) - prefer an explicit time window. Progress<T> captures context at construction, never conflates, and can deliver out of order so counters run backwards. Make thread affinity structural: start the loop on the UI thread and never ConfigureAwait(false). Static events leak handlers. Native-backed rendering turns churn into a use-after-free hazard, and a native AV bypasses every managed handler, so an empty crash log says nothing about threading - WER LocalDumps is the tool. Ends with a checklist for a new WPF app.","caveats":["Distilled from one app (charts over LiveCharts/SkiaSharp, CommunityToolkit.Mvvm 8.4, .NET 10) - the platform facts generalize, the emphasis reflects that codebase.","DataTemplate resolution walking the type hierarchy for implicit templates is asserted from reasoning, not tested here; verify before relying on a base-class template."],"tags":["wpf","csharp","ui","architecture","concurrency","debugging"],"links":["pattern.change-coalescing","script.set-crash-dump-capture"]}]}
