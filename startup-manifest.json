{
  "$comment": "Startup contract for JanetHome. See DESIGN-NOTES.md section 1. Every entry either resolves or startup fails loudly. Do not soften onMissing.",
  "version": 1,
  "base": "JanetBase",
  "order": "sequential",
  "onMissing": "fail",

  "$comment.read": "Kept to what the session cannot retrieve, because the read list is billed on EVERY start while the graph is billed only when asked. PROVENANCE.md left on 2026-08-08: its contract is already the boundary rule below, its detail is file.provenance, and its own manifest reason said 'read before adding anything' -- conditional by its own description, which is the definition of retrievable. README is the bootstrap: it must survive alone, because it is what tells the session the graph exists.",

  "read": [
    {
      "path": "README.md",
      "why": "Bootstrap: how to start, the boundary, and that research.json is the entry point for everything else"
    },
    {
      "path": "DESIGN-NOTES.md",
      "why": "The transferable patterns. Operating rules live here, not in a system prompt"
    }
  ],

  "run": [
    {
      "cmd": "scripts\\Show-ThreadItems.ps1",
      "captureAs": "threadStack",
      "why": "Unfinished investigations from the last session. Small, stateful, and useless if fetched late -- so it runs at startup rather than on demand"
    }
  ],

  "$comment.retrieval": "The tool inventory is deliberately NOT loaded here. research.json holds every script, pattern, note, and file as a node; startup carries only the pointer below and the session pulls the two or three nodes it needs. See DESIGN-NOTES section 2.",
  "retrieval": {
    "graph": "research.json",
    "via": "scripts\\Get-Research.ps1",
    "$comment.via": "'via' stays a path because startup resolves it and a broken pointer must fail loudly. The script is now a thin shim over the same code the MCP server and the CLI run, so it is still a correct answer -- just no longer the only one. 'tools' below names the other two. Do not put a tool name here: it would stop resolving, and startup would report a problem that is not one.",
    "tools": "Three front ends over one implementation (Janet.Core), so they cannot disagree: the research_query / research_add / research_update / research_rename MCP tools; 'janet research query|add|update|rename' on the command line; and the scripts, which now shim to the CLI. Use the MCP tools when they are connected -- no process start, and the schema is typed. Hooks and other shell callers need the CLI, which is why it is not optional.",
    "hint": "Run it with no arguments for kinds and tags; -Query for scored search, -Tag / -Id for exact lookup, -? for the full surface. Responses report their own truncation. The research_* MCP tools are the same code.",
    "$comment.hint": "'hint' is the only prose from this block that reaches the trimmed brief. The fields below are reference material, emitted under -Full -- keep them accurate, but do not grow 'hint'. See notes\\startup-brief-budget.md.",
    "$comment.json": "These scripts emit compressed JSON by default -- the consumer is a model, not a terminal. Node boundaries are unambiguous, new schema fields arrive without a formatter change, and nothing is spent on alignment no reader uses. -Text opts into the formatted view; -Pretty indents the JSON for debugging.",
    "usage": [
      "Get-Research.ps1                          -- kinds and tag index only (cheap orientation)",
      "Get-Research.ps1 -Query 'thread stack'    -- scored top 5. -First N / -All to widen",
      "Get-Research.ps1 -Tag thread-stack        -- by tag (never truncated)",
      "Get-Research.ps1 -Id pattern.thread-items -Expand  -- a node plus its linked neighbours",
      "Get-Research.ps1 -Kind script             -- every node of a kind",
      "Add -Text for a human-readable form at the terminal; -Pretty to indent the JSON."
    ],
    "envelope": "Queries return { returned, totalMatches, truncated, nodes[] } -- check 'truncated' before concluding you have seen everything. An empty result uses the same shape, so never special-case it.",
    "add": "research_add, 'janet research add', or scripts\\Add-ResearchNode.ps1 -Id <kind>.<slug> -Kind <kind> -NodePath <path> -Summary '<one line>' -Tags a,b -Links id1,id2 -Caveats 'what bites you'. Validates and splices; never hand-edit research.json. -DryRun to preview.",
    "update": "research_update, 'janet research update', or scripts\\Update-ResearchNode.ps1 -Id <id> [-Summary|-Tags|-Links|-Caveats|-ScriptParams ...] [-Append] [-Remove field]. Preserves every field you do not name, including ones it has never heard of. Returns the before/after of each change. -DryRun to preview.",
    "caveats": "Nodes may carry a 'caveats' array -- missing dependencies, external services, platform limits, outright breakage. Retrieval always prints them prefixed with '!'. Read them before relying on a node; several scripts in this repo are known-broken and say so.",
    "fallback": "scripts\\Get-ScriptCatalog.ps1 scans the script directory directly, for when a lookup misses or research.json is stale"
  },

  "$comment.rules": "ENFORCED rules are backed by a mechanism that fails: startup validation, a hook that denies the tool call, or a linter that exits 1. ADVISORY rules hold only as long as the agent chooses to honour them -- they are marked so, because a rule claiming enforcement it does not have is worse than an honest suggestion. Do not rationalise past an advisory rule on the grounds that this particular case is small; that is the exact failure these were written after.",

  "$comment.rules.enforcedBy": "A rule claiming ENFORCED may carry 'enforcedBy': the hook script that backs it, as the hook itself resolves it -- Join-Path <project dir> <enforcedBy>. Startup tests that path against the CURRENT project dir (CLAUDE_PROJECT_DIR, else cwd), because that is the only place the hook will look. If it does not resolve the hook is not wired, the rule is emitted as ADVISORY with a note, and the mismatch is listed under 'problems'. It does not fail startup: working in JanetHome from another repo's project dir is legitimate, and a brief that tells the truth is worth more than one that refuses to start. Discovered 2026-08-01, when both hook-backed rules read ENFORCED in a session where neither hook was loaded.",

  "$comment.rules.shape": "Each rule is { text, why }. 'text' is the imperative and is always emitted; 'why' is the justification and appears only under -Full or -Text. The split keeps the reasoning on disk and reviewable without billing it to every session -- a bare imperative is easier to argue past, which is what the comment above warns against. Plain strings still parse, for anything that does not decompose. Keep 'text' to one line; put the argument in 'why'.",

  "rules": [
    {
      "text": "ENFORCED: never edit research.json directly -- use the research_* MCP tools, 'janet research', or the Add-/Update-ResearchNode scripts.",
      "why": "A PreToolUse hook denies Write and Edit against it. All three entry points splice into the file's existing text rather than reserializing it, which is what keeps the hand-curated grouping and comment keys alive; a direct edit is refused because it is the one path with no validation behind it.",
      "enforcedBy": "scripts\\Invoke-EditGuard.ps1"
    },
    {
      "text": "ENFORCED: fix what the PowerShell linter reports before moving on.",
      "why": "A PostToolUse hook lints every .ps1 you write or edit against notes\\powershell-house-rules.md.",
      "enforcedBy": "scripts\\Invoke-LintHook.ps1"
    },
    {
      "text": "ENFORCED: startup fails if any manifest entry does not resolve.",
      "why": "A broken contract stops the session rather than degrading it quietly."
    },
    {
      "text": "ENFORCED: research first -- ask the catalog before building a tool or hand-rolling a technique.",
      "why": "A PreToolUse hook denies Write of a NEW .ps1 into a scripts directory unless Get-Research.ps1 has run recently, and the denial arrives with the closest existing nodes already listed. Written 2026-08-11 after Get-AssemblyApi.ps1 was authored while Get-ApiDoc.ps1 sat in the catalog answering the same question -- nothing was violated, because until now the manifest explained HOW to query the catalog and never said TO. Advisory would not have covered it: 'I only need one type name' is exactly the rationalisation the edit guard was written about.",
      "enforcedBy": "scripts\\Invoke-ResearchGuard.ps1"
    },
    {
      "text": "ADVISORY: graph first, narrative second -- once you know WHAT you are building.",
      "why": "Query a semantic model for the narrowest slice that answers the question; read source only to spot-check the claims that matter (DESIGN-NOTES section 4). This is a technique for working inside a problem, and it ranks BELOW research-first: graphing tells you how the code you are about to touch fits together, while the catalog tells you whether to touch it at all. Reaching for the graph -- or worse, for grep -- before asking what already exists is the expensive order."
    },
    {
      "text": "ADVISORY: subagent prompts for code analysis must carry explicit RazorGraph instructions -- tool names to load via ToolSearch, the graphId or saved-graph path, and the graph-first rule itself.",
      "why": "Spawned agents inherit the tool surface but not the operating rules: nothing tells them the graph server exists, so they fall back to grep and speculation. Observed 2026-08-11 -- three exploration agents ran text-first until instructed mid-flight; their graph-verified second passes found facts the text passes missed (a live same-seed pairing site, complete consumer sets). The rule the session honours does not propagate by osmosis; it must be written into every prompt that delegates analysis."
    },
    {
      "text": "ADVISORY: before an expensive query, name what you are trying to learn and the cheapest query that distinguishes the hypotheses.",
      "why": "Agents default to broad exploratory queries that time out or silently truncate. Making hypothesis-formation a prerequisite converts flailing into bisection (section 7)."
    },
    {
      "text": "ADVISORY: add a thread item for work you surface but are not doing now; switch focus deliberately, and complete rather than abandon.",
      "why": "Debugging is depth-first and working memory is not, so the list is the unwind path. It replaced a push/pop stack on 2026-08-08: adding no longer displaces what is active, items can be amended in place, and completing retains the item instead of deleting it -- so recording work costs nothing and finishing it leaves a record (section 3)."
    },
    {
      "text": "ADVISORY: for multi-file mechanical edits, emit a JSON plan and run Invoke-SurgicalEdit.ps1 rather than hand-editing.",
      "why": "Models decide well and perform dozens of mechanical edits unreliably. The plan is reviewable before anything touches disk (section 5)."
    },
    {
      "text": "ADVISORY: wrap known-flaky external tools in an explicit timeout and fallback.",
      "why": "A hung call is indistinguishable from thinking -- the worst failure mode, because it produces no output and no diagnosis (section 8)."
    },
    {
      "text": "ADVISORY: nothing employer-SPECIFIC goes in this repo -- their source, their system topology, production telemetry, security-posture detail, colleague personal data. Rebuilding a general pattern from principles is fine even if you first built one there: the test is derivation, not resemblance.",
      "why": "Judgement only; no mechanism enforces it, since the identifier list that would is itself employer information. Not a per-edit gate -- the 2026-07-27 extraction settled what was carried over, and the author who made that call is not the risk. The live failure mode is an AGENT with no memory of that decision reproducing something employer-specific from a description. But 'architecture' was too broad a word for that and is gone: this repo's own flagship is a clean-room rebuild (DESIGN-NOTES section 4 -- the Roslyn graph server, employer work product left behind, rebuilt from the pattern), and an SWR cache or an LSP-backed tool is a general technique that belongs to the field, not to whoever you last built one for. PROVENANCE standard 5 already drew the line correctly: 'not coupled to employer systems, and not derived from proprietary internals.' Derivation is the test; resemblance is not evidence of it. PROVENANCE.md is the standard and the record, retrievable as file.provenance."
    },
    {
      "text": "ADVISORY: scripts target PowerShell 7. Add -Target Ps51 when one may run on stock Windows 5.1.",
      "why": "The hook lints at the Ps7 default, and the 5.1 differences fail in ways that print success. Rules and rationale: notes\\powershell-house-rules.md."
    }
  ]
}
